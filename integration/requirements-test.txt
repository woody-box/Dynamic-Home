# Test harness for the HA integration tests.
# Pinned to the versions validated locally (resolves Home Assistant 2024.3.3
# on Python 3.11). The harness pulls a compatible pytest / pytest-asyncio.
pytest-homeassistant-custom-component==0.13.109
tzdata
