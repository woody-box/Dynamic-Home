name: tests

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  dynamic-home-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements-test.txt

      - name: Run engine + integration tests
        run: pytest -q

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install ruff
        run: pip install ruff

      - name: Ruff
        run: ruff check custom_components tests

  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Hassfest
        uses: home-assistant/actions/hassfest@master

      # HACS validation is advisory here: the repo is private and some checks
      # are about public-store publishing (topics, brands...). It still runs and
      # surfaces findings, but does not block CI.
      - name: HACS validation
        uses: hacs/action@main
        continue-on-error: true
        with:
          category: integration
          ignore: brands
